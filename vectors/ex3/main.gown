package main

import (
    "fmt"
)

\\\\observer fmt.Printf

type wheel struct {}

type bicycle struct {
    front  \iso *wheel
}

// dispose of goner *wheel
func puncture(goner \iso *wheel) {}

func main() {
  j := \new(bicycle{
       front: &wheel{},
  })

  var a \iso *wheel
  a, j.front = j.front, nil

  fmt.Printf("a='%#v'", a)
}
